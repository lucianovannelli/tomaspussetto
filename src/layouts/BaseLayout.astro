---
import { pwaInfo } from 'virtual:pwa-info';
import { ViewTransitions } from 'astro:transitions';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Tomás Pussetto - Método ENDS. Tu entrenamiento personal.' } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <meta name="description" content={description} />
    <meta name="application-name" content="Tomás Pussetto" />
    
    <!-- iOS PWA Standalone Mode Meta Tags -->
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="apple-mobile-web-app-title" content="Tomás Pussetto" />

    <meta name="theme-color" content="#26160d" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" sizes="192x192" href="/icons/icon-192.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png?v=2" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
    <title>{title}</title>
    {pwaInfo && <Fragment set:html={pwaInfo.webManifest.linkTag} />}
    <ViewTransitions />
  </head>
  <body>
    <div id="demo-banner" class="hidden fixed top-[calc(env(safe-area-inset-top)+0.5rem)] left-1/2 -translate-x-1/2 z-50 bg-[#26160d] text-[#f5f0e8] text-[10px] font-black uppercase tracking-widest px-3.5 py-1.5 rounded-full shadow-xl border border-[#e6dfd5]/30 flex items-center gap-1.5 pointer-events-none select-none">
      <span class="w-1.5 h-1.5 rounded-full bg-amber-400 animate-pulse"></span>
      Modo Demo / Ejemplo
    </div>
    <slot />
    <script>
      import { registerSW } from 'virtual:pwa-register';
      registerSW({ immediate: true });

      function updateDemoBanner() {
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('demo') === 'true') {
          localStorage.setItem('tp_demo_mode', 'true');
        }
        
        const isDemo = localStorage.getItem('tp_demo_mode') === 'true' || localStorage.getItem('ksc_demo_mode') === 'true' || import.meta.env.PUBLIC_KSC_DEMO_MODE === 'true';
        const banner = document.getElementById('demo-banner');
        if (banner) {
          if (isDemo) {
            banner.classList.remove('hidden');
          } else {
            banner.classList.add('hidden');
          }
        }
      }

      // Run on load and page transition
      document.addEventListener('astro:page-load', updateDemoBanner);
    </script>
  </body>
</html>

